// This hook is now deprecated and is replaced by the `useData` context hook.
// This file is kept to avoid breaking imports in the codebase during transition,
// but all components should be migrated to use `useData` from `context/DataContext.tsx`.

export {};
